#include <iostream>

using namespace std;

int main()
{
    int count, x,y;
    cin >> count >> x >> y;

    if (count < 10)
    if (x < y)
    {
    cout << x << " is less than " << y << endl;
    }
    else
    {
    cout << x << " is greater than " << y << endl;
    }

    return 0;
}
